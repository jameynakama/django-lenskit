[project]
name = "django-lenskit-ai-query"
version = "0.1.0"
description = "Read-only AI query helper for Django Admin (strict JSON DSL → validated ORM)"
requires-python = ">=3.10"
dependencies = [
    "django>=4.2",
]

[project.optional-dependencies]
test = [
    "pytest>=7.0",
    "pytest-django>=4.7",
    "pytest-mock>=3.12",
    "pytest-cov>=4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true
